1. Run curl.sh or commandtool.sh to test basic functions
2. Enter "server name" and "port"
3. Type "quit" to create a new certificate file
4. See result at /tmp/Result
5. Add data into file 'forpoll.data'
6. Run pollhost.sh to populate Host specific data and test pollhosts interface
7. Enter "server name" and make sure 'certfile.cer' in the same folder

